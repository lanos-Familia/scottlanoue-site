{
  "mcpServers": {
    "letta": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "letta-mcp-server"],
      "env": {
        "LETTA_BASE_URL": "https://letta.lobster-hake.ts.net",
        "LETTA_PASSWORD": "${LETTA_SERVER_PASSWORD}"
      }
    }
  }
}
